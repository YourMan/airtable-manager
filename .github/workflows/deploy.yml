name: Deploy to VPS

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST_IP }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # 1. Go to the project folder
            cd /var/www/globalautomation
            
            # 2. Pull the latest code
            git pull origin main
            
            # 3. Install dependencies (Uncomment if needed)
            # npm install 
            # pip install -r requirements.txt
            
            # 4. Restart the app (Update this for your specific app)
            # pm2 restart all
            # systemctl restart nginx

